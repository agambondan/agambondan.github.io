import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.agambondan.cv",
  appName: "Firman Agam CV",
  webDir: "dist",
  server: {
    androidScheme: "https"
  }
};

export default config;
